part of '../../yandex_js_maps.dart';

/// Enum defining available Yandex map types.
enum MapType {
  /// Standard map view.
  map,

  /// Satellite imagery view.
  satellite,

  /// Hybrid view (satellite + labels).
  hybrid,
}
